헤어파일에서 templte 를 써서 class 를 만들고 난 후 함수를 정의하고 .cpp 파일에서 구현할 경우 에러가 발생한다.

complier 가 해석하는데 있어서 발생하는 문제.

1. 해결법은 헤더파일에다가 모든 함수를 구현하는 것 
2. 함수를 inline 으로 정의하는 것.
3. 사용처에서 .cpp 파일을 include 하는 것.
4. 함수를 모든 변수 type 대해서 구현해 놓는 것.

위와같이 3가지이다. 2, 3, 4 번에 대해서는 아직 자세히 확인해 보지 못했다.
